name: Prisma Migrate

on:
  workflow_dispatch: {}
  push:
    branches: ["main"]
    paths:
      - "prisma/**"
      - "package.json"
      - "package-lock.json"
      - "prisma.config.ts"
      - ".github/workflows/prisma-migrate.yml"

concurrency:
  group: prisma-migrate-main
  cancel-in-progress: false

jobs:
  migrate:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    env:
      DATABASE_URL: ${{ secrets.DATABASE_URL }}   # 6543 transaction pooler
      DIRECT_URL:   ${{ secrets.DIRECT_URL }}     # 5432 session pooler
      DOTENV_CONFIG_QUIET: "true"

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Validate environment variables
        run: |
          if [ -z "$DATABASE_URL" ]; then
            echo "Error: DATABASE_URL secret is not set in repository secrets"
            exit 1
          fi
          if [ -z "$DIRECT_URL" ]; then
            echo "Error: DIRECT_URL secret is not set in repository secrets"
            exit 1
          fi
          echo "DATABASE_URL is set (length: ${#DATABASE_URL})"
          echo "DIRECT_URL is set (length: ${#DIRECT_URL})"

      - name: Prisma generate
        run: npx prisma generate

      - name: Prisma migrate deploy
        run: npx prisma migrate deploy

      - name: Prisma migrate status
        if: always()
        run: |
          if [ -z "$DATABASE_URL" ]; then
            echo "Skipping migrate status: DATABASE_URL is not set"
            exit 0
          fi
          npx prisma migrate status
